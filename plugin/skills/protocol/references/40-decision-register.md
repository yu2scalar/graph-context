<!-- generated by graph_tool render (public-decisions) from dependency_graph.json + docs/entities — do not edit -->

# Public decision register (generated from each decision's Public summary)

| id | Decision | Supersedes / resolves | Status |
|----|----------|-----------------------|--------|
| D1 | v1: closed node objects, open root | — | folded into D13 |
| D2 | Node ids pattern-constrained (`^[a-z0-9][a-z0-9_-]*$`), applied to ids, keys, `current_node`, edges | — | active |
| D3 | JSON Schema draft 2020-12 | — | active |
| D4 | Ship a minimal seed template | — | active |
| D5 | Registry mapping lives in `config.registries`; project CLAUDE.md carries prose nuance + a pointer | — | active |
| D6 | Bounded footprint: fixed path list, marked blocks, install/uninstall, sha256 snapshot, rule R6 | — | active |
| D7 | Compaction = fold superseded decisions / resolved issues into the survivor via `folded[]`; proposal-only | — | active |
| D8 | Typed edges `resolves` and `supersedes` | — | active |
| D9 | `growth_threshold` default 5, tunable; init idempotent, preserves human decisions; `--reset-structure` | — | active |
| D10 | `source_ref` single-valued on decision/issue nodes only; growth measured on the feature side | — | active |
| D11 | `code_roots` not in config; derived from component nodes | — | active |
| D12 | Component layer under Root; R7 components always visible; R8 no reinvention | — | active |
| D13 | Root closed: `current_node`, `nodes`, `config` only | — | active |
| D14 | Staleness check (code newer than docs, missing paths, unknown `source_ref`) | — | active |
| D15 | First application target: the skill's own repo, then a real project | — | active |
| D16 | Thin delegating skills per command | — | folded into D27 |
| D17 | Component detection = top-level directory listing; S3 governs `code_targets` derivation | resolves u1 | active |
| D18 | S5 absorbed into R1 | resolves u2 | active |
| D19 | Handover template hardening: legend, "Resolved this session" line, who/when, local-only marker | — | active |
| D20 | Package as a Claude Code plugin (marketplace + `plugin/`); skill files never enter the project; paths via `${CLAUDE_SKILL_DIR}` / `${CLAUDE_PLUGIN_ROOT}`; footprint = graph + handover + 2 marked blocks | — | active |
| D21 | Delegate skills renamed to `install/uninstall/init/hydrate/handover/compact` under the plugin namespace; bare `/graph-*` is impossible for plugin skills | — | folded into D27 |
| D22 | Protocol skill renamed `graph-context-sync` → `protocol` (was registering as `graph-context-sync:graph-context-sync`) | — | active |
| D23 | Plugin name shortened to `graph` (marketplace stays `graph-context-sync`): commands read `/graph:hydrate`, `/graph:init`, …; install string `graph@graph-context-sync` | — | folded into D27 |
| D24 | Content-based staleness in addition to timestamps: registry ↔ graph (unindexed ids, orphan `source_ref`, lost `folded` ids), docs-only nodes compared with the code of their parent / affected nodes, shipped registers treated as code + registry | resolves u12 | active |
| D25 | Handover §2 tables, §6 counts/results and §7 timestamps are generated from the graph, never retyped; manual rows marked `(manual)` | resolves u13 | active |
| D26 | Protocol as code: `tools/graph_tool.py` executes R1, hydrate, check (growth/fold/staleness), handover tables, fold, split; rule R9 — every id/count/timestamp shown comes from the tool. Rationale: every forgotten update in the authoring session was caught by a mechanical check or a peer, never by the author | — | active |
| D27 | Repositories re-created (user, 2026-09-24): public `yu2scalar/graph-context` = the plugin only, marketplace `graph-context`, install `graph@graph-context`; private `yu2scalar/graph-context-dev` = project root holding the design records (`CLAUDE.md`, `docs/`, `dependency_graph.json`, `.context/graph_tool.log`) with the public repo as submodule `public/`. Marker strings renamed to `graph-context:begin/end`. Public history restarts at 3.2.0; the pre-3.2.0 history (hashes cited in the private plans) is kept as `.archive/graph-context-sync-history.bundle` in the private repo; the old remote `graph-context-sync` is to be deleted later. Reason (user): the records the skill must keep accurate had no git history because design and public skill shared one repo | resolves u5, u16 | active |
| D28 | Issues become graph nodes (retroactively, via an append-only issue registry and `add-node`); decisions link to them with `resolves` (user decision 2026-09-24; implementation planned, see private plan design-shift) | — | active |
| D29 | Decision content lives in one append-only file per decision; nodes carry only `source_ref`; a generated `CURRENT.md` lists the active decisions (the current specification); this register becomes a generated table | supersedes d5 | active |
| D30 | No authored handover: `current_node` is the handover, `/graph:hydrate` output is the view, `/graph:handover` becomes a completion gate; work-in-progress state is recorded as an issue, not prose | supersedes d19, d25 | decided, not yet implemented |
| D31 | Fold hides instead of deleting: folded decisions/issues stay as nodes under the survivor (`supersedes` / `resolves` edges, status `folded`), hidden from default hydrate output | supersedes d7 | decided, not yet implemented |
| D32 | Storage stays files (`dependency_graph.json` + Markdown) as the source of truth; a later Graph DB migration is kept mechanical by four conditions: every write through `graph_tool.py`, permanent ids, fixed headings in decision/issue files, the operations log kept; the first step would be a rebuildable read-only DB index generated from the files | — | active |
| D33 | Backlog model (resolves U26): problems/questions = `issue` nodes; not-yet-started work = `feature`/`function` nodes with `wip_status: PLANNED`; `next` flag on both; the Backlog is a graph-wide *view* (open issues OR PLANNED/IN_PROGRESS/BLOCKED OR next), its filter selectable (config default + per-call options) and whatever it excludes printed as a count per component | resolves u26 | active |
| D34 | Working rules: R-a record, then ask — a question to the user is asked only after the node it concerns is written, citing the node id, and the answer updates that node; R-b plans are nodes — every not-yet-started step is a PLANNED node `part_of` its feature, ordered by `depends_on`, the next one flagged `next` | — | active |
| D35 | Issue state is stored on the issue node: `issue_status` open | resolves u33 | active |
| D36 | Integrity-first store: each decision, issue, plan and rule is one entity file written only by graph_tool (validated before saving, sha256-checked); status and edges live only in the graph; registers and other readable documents are generated; adding an entity first lists the existing ones so duplicates are caught. | supersedes d29 | active |
| D37 | Fold hides instead of deleting: every earlier folded decision / issue is restored as a hidden history node linked by supersedes / resolves; the folded[] field is dropped; resolved issues are hidden by default, transferred ones stay visible. | resolves u41, u42, u43 | decided, not yet implemented |
| OP1 | — | — | folded into D5 |
| OP2 | — | — | folded into D10 |
| OP3 | — | — | folded into D11 |
| OP4 | — | — | folded into D13 |
| S1 | `dependency_graph.json` lives at the project root | — | active |
| S2 | v1: single SKILL.md, `/graph-*` as sub-commands | — | folded into D27 |
| S3 | init scans design docs + referenced code, no blind source walk | — | folded into D17 |
| S4 | Skill text and stored artifacts in English | — | active |
| S5 | Every edge target must exist | — | folded into D18 |
